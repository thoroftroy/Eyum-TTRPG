
## TODO
- Generate images for each race (just use GIMP and make a really bad version of it, it just had to show generally what it looks like)
- Make more monsters
- Fix character manager to add graphs for each classes and the average to make it super easy to visualize the data (adding a gui to actually visualize data)
	- Preferably line graphs
	- Make the data as readable as possible, show a summary at the bottom of the screen, hovering over one data point will highlight its line on the graph
	- By default all lines are shown (probably have it set to be each class as a line and each level is a point on the x axis and on  the y we can change what it is between all tracked stats) each class and the average need their own lines
	- Clicking on a node in the bottom summary will hide the line from the graph or show it (toggle)
	- This program should run seperatly from the genrator but it should run the generator on boot to make sure its data is up to date and accurate
	- The goal is just to make the data as easy to visualize as humanly possible, i want to see where everything peaks and what happens level by level for each class and so on all at once
	- Sense we are adding a gui to view the data it would be good to let us modify the generator and run it in the same program, for exmaple on the top bar of the program let me click a button to just rerun the generator, have information displayed giving a progress report and such. Also let me modify the .json data settings in the program, let me run the program with the modified json files and optionally save them to the files but also an option to not save them that way it wont automatically overwrite everything, also allow it to restore the previous version of the settings .json files so i can test lots of stuff and see what happens. The graphs should update in real time as we go. Put the settings things in tabs so on the top bar there can be a settings tab which shows all of the settings .json files on a left side bar view and i can click on them and edit them like in a text document, it should have the controls visable in this scene, the top bar should also let me go back to the graph view or just rerun it at any time. 